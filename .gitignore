# Secrets — never commit
.env

# The Obsidian vault is personal data, not source code
JARVIS VAULT/

# Runtime state (event logs, SQLite)
data/

# Dependencies & build output
node_modules/
dist/
.venv/
__pycache__/

# Downloaded ML models (re-fetched by setup; ~60MB+)
services/voice/models/

# OS / editor noise
.DS_Store
*.log
.claude/
